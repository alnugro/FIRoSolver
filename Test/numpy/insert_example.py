import numpy as np
import math

# Initial array
arr = np.array([1, 2, 5, 6])

# Insert 4 between 2 and 5 (index 2)
arr_with_new_value = np.insert(arr, 0, 4)

print(arr_with_new_value)

test = 1.5

print(math.floor(test))